import { ScrolableDirective } from './scrolable.directive';

describe('ScrolableDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolableDirective();
    expect(directive).toBeTruthy();
  });
});
